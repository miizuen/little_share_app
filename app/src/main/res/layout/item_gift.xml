<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Gift Image (viết trước) -->
        <ImageView
            android:id="@+id/ivGift"
            android:layout_width="0dp"
            android:layout_height="140dp"
            android:scaleType="centerCrop"
            android:src="@drawable/gift_teddy_bear"
            android:contentDescription="Gấu bông Teddy"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- HOT Badge (viết SAU → hiển thị trên ảnh) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/tvHotBadge"
            android:layout_width="60dp"
            android:layout_height="30dp"
            android:backgroundTint="#FEF9C3"
            android:elevation="8dp"
            android:fontFamily="sans-serif-medium"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:text="🔥HOT"
            android:textAllCaps="false"
            android:textColor="#CA8A04"
            android:textSize="12sp"
            android:textStyle="bold"
            android:visibility="visible"
            app:cornerRadius="12dp"
            app:iconGravity="textStart"
            app:iconPadding="4dp"
            app:iconTint="#CA8A04"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Out of Stock Overlay -->
        <View
            android:id="@+id/viewOutOfStockOverlay"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="#CCCCCC"
            android:alpha="0.7"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@id/ivGift"
            app:layout_constraintBottom_toBottomOf="@id/ivGift"
            app:layout_constraintStart_toStartOf="@id/ivGift"
            app:layout_constraintEnd_toEndOf="@id/ivGift" />

        <!-- Out of Stock Label (MaterialButton) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/tvOutOfStock"
            android:layout_width="118dp"
            android:layout_height="33dp"
            android:layout_marginBottom="56dp"
            android:backgroundTint="#000000"
            android:fontFamily="sans-serif-medium"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:text="Hết hàng"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:visibility="gone"
            app:cornerRadius="16.5dp"
            app:layout_constraintBottom_toBottomOf="@id/ivGift"
            app:layout_constraintStart_toStartOf="@id/ivGift"
            app:layout_constraintEnd_toEndOf="@id/ivGift" />

        <!-- Gift Name -->
        <TextView
            android:id="@+id/tvGiftName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Gấu bông Teddy"
            android:textColor="#333333"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="12dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/ivGift" />

        <!-- Points -->
        <!-- Points -->
        <TextView
            android:id="@+id/tvPoints"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🌟 +800"
            android:textColor="#FF6B35"
            android:textSize="16sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvGiftName" />

        <!-- THÊM MỚI: Số lượng còn lại -->
        <TextView
            android:id="@+id/tvQuantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Còn 9/15"
            android:textColor="#666666"
            android:textSize="12sp"
            android:fontFamily="sans-serif"
            android:layout_marginTop="2dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvPoints" />

        <!-- Exchange Button (Gradient) - CẬP NHẬT constraint -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnExchange"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_button_gradient"
            android:fontFamily="sans-serif-medium"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:text="Đổi ngay"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            app:backgroundTint="@null"
            app:cornerRadius="24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvQuantity" />

        <!-- Out of Stock Button - CẬP NHẬT constraint -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOutOfStock"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginTop="8dp"
            android:backgroundTint="#999999"
            android:enabled="false"
            android:fontFamily="sans-serif-medium"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:text="Hết hàng"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            app:cornerRadius="24dp"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvQuantity" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>