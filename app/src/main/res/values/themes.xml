<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Little_share" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Little_share" parent="Base.Theme.Little_share" />
    <style name="ThemeOverlay.CustomToggleButton" parent="">
        <item name="materialButtonOutlinedStyle">@style/Widget.Custom.MaterialButton.OutlinedButton</item>
    </style>

    <style name="Widget.Custom.MaterialButton.OutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">#673AB7</item>
        <item name="strokeWidth">1.5dp</item>
        <item name="cornerRadius">20dp</item>
        <item name="rippleColor">#33885ED7</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="backgroundTint">#EF7C8EF0</item>
        <item name="android:textColor">@color/background_light</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
        <item name="tabIconTint">@null</item>
        <item name="tabIndicatorColor">@android:color/transparent</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabTextColor">#666666</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
    </style>

    <style name="CustomTabTextAppearance" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:fontFamily">@font/inter</item>
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="CustomSegmentedButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>

        <!-- Text -->
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/inter</item>

        <!-- Bo góc -->
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item>

        <!-- Icon -->
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">8dp</item>

        <!-- Mặc định -->
        <item name="rippleColor">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="backgroundTint">@color/white</item>
    </style>

</resources>